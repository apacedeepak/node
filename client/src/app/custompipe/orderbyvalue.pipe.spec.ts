import { OrderbyvaluePipe } from './orderbyvalue.pipe';

describe('OrderbyvaluePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderbyvaluePipe();
    expect(pipe).toBeTruthy();
  });
});
