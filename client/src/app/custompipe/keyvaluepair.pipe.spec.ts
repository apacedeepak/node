import { KeyvaluepairPipe } from './keyvaluepair.pipe';

describe('KeyvaluepairPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyvaluepairPipe();
    expect(pipe).toBeTruthy();
  });
});
