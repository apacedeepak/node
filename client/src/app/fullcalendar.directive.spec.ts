import { FullcalendarDirective } from './fullcalendar.directive';

describe('FullcalendarDirective', () => {
  it('should create an instance', () => {
    const directive = new FullcalendarDirective();
    expect(directive).toBeTruthy();
  });
});
